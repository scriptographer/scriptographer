include("raster.js");if (initRaster()) {    values = prompt("Enter Raster Values:", {type: "number", value: 10, title: "Grid Size:", width: 50},{type: "number", value: 10, title: "Stroke Scale:", width: 50});    if (values) {        var size = values[0], scale = values[1];        function drawDot(x, y, dot, radius) {            var art = dot.clone();            art.style.stroke.width = radius * scale;            var m = new Matrix();            m.rotate(radius * Math.PI * 2);            m.translate(x * size, y * size);        /*            if ((y % 2) == 0) {                m.translate(size / 2 , 0);            }        */            art.transform(m);             return art;        }        doRaster();    }}