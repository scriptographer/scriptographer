<%= $members prefix=$title %>

<% $members -%>
<% foreach $member in param.members prefix='<div class="documentation-paragraph">' suffix='</div>' %>
<%= $member.renderMember(param) -%>
<% end %>

<% $title -%>
<%= param.title prefix=settings.section2Open suffix=settings.section2Close %>
