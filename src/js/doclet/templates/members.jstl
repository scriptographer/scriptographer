<%= $members prefix='<div class="documentation-members">' suffix='</div>' %>

<% $members -%>
<% foreach $member in param.members prefix=<%= param.title | heading 2 %> %>
<%= $member.renderMember(param) -%>
<% end %>
